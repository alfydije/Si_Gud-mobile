import 'package:flutter/material.dart';

class DataBarangScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Data Barang"),
      ),
      body: Center(
        child: Text("Data Barang Screen"),
      ),
    );
  }
}
