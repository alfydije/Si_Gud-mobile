import 'package:flutter/material.dart';

class BarangKeluarScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Barang Keluar"),
      ),
      body: Center(
        child: Text("Barang Keluar Screen"),
      ),
    );
  }
}
